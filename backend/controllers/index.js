"use strict";
const { User } = require("../models/index");

class Controller {
  static async login(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }

  static async register(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }
}

module.exports = {
  Controller,
};
